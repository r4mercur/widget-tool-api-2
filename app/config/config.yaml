# Database settings
database_uri:

# HTTP settings
http_host: 0.0.0.0
http_port: 5999

threads: 12

# General sharing settings for internal DSP use
general_sharing_settings:
  http_user:
  http_password: